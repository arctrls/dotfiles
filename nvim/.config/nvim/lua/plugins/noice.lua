return {
  {
    "folke/noice.nvim",
    opts = {
      presets = {
        -- Disable bottom search to use popup for search
        bottom_search = false,
        -- Keep command palette in center
        command_palette = true,
      },
    },
  },
}